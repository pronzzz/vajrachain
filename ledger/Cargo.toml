[package]
name = "ledger"
version = "0.1.0"
edition = "2021"

[dependencies]
crypto = { path = "../crypto" }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
thiserror = "1.0"
hex = "0.4"
blake3 = "1.5"
