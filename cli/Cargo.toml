[package]
name = "cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
ledger = { path = "../ledger" }
crypto = { path = "../crypto" }
hex = "0.4"
rand = "0.8"
ed25519-dalek = "2.0"
